# CR-YYYY-MM-DD-short-slug

## Title
(One line — what this change is.)

## Why
(The actual motivation — what problem this solves or what it enables.)

## Scope
- Files/pages expected to change:
- New Supabase tables/columns expected (list them — do not apply migrations, just
  propose the SQL in the PR):
- Anything explicitly out of scope (helps prevent unwanted scope creep):

## Plan
(Step by step, as concrete as possible. Reference existing patterns in the codebase
to reuse rather than reinvent — e.g. "use the same collapsible op-section pattern
already in businesses.html" rather than building a new one.)

## Verification required before the PR is marked ready
- [ ] Syntax-check every changed `.html`/`.js` file
- [ ] If a live page is affected: deploy to a preview/branch context if possible, or
      note in the PR that live verification still needs to happen post-merge
- [ ] Playwright check of the actual rendered result — console errors, expected
      elements present — not just "the code looks right"

## Risk level
(Low / Medium / High, and why.)

## Anything you're unsure about
(Flag it here rather than guessing silently. A note in the PR is fine — a guessed
answer baked into the code is not.)
